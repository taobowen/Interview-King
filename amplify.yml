version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        # Create .env.production with environment variables for SSR runtime
        - echo "FIREBASE_SERVICE_ACCOUNT_KEY=$FIREBASE_SERVICE_ACCOUNT_KEY" >> .env.production
        - echo "DATABASE_URL=$DATABASE_URL" >> .env.production
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*